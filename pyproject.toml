[project]
name = "netrunner-rules"
version = "0.1.0"
description = "Document generator to convert a YAML description of rules of Netrunner"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
]
